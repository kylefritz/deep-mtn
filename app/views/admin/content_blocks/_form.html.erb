<%= semantic_form_for [:admin, @content_block], builder: ActiveAdmin::FormBuilder do |f| %>
  <%= f.inputs :page, :name, :content_type, :title %>
  <%= f.file_field :images, multiple: true, direct_upload: true %>
  <%= f.actions %>
<% end %>
